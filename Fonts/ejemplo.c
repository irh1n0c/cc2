#include "chess.h"
#include "figures.h"
#include "gc.h"

void display(){
  int flow;
  char string;
  char** blackSquare = reverse(whiteSquare);
  interpreter(blackSquare);
  probado por jose;
  template;
  garbageCollector();
}
